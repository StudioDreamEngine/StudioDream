-- meant to seperate the text and image handling so that we can have a shared button class and stuff like that
-- idk what to call this tho, so unused for now cuz of that
local UIRender = {}

function UIRender.CreateText()
    local Text = {}

    function Text.PassProperties(self)
        
    end

    function Text.Draw()
        
    end

    return Text
end

return UIRender