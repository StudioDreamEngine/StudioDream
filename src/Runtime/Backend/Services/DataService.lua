local DataService = {}

function DataService.CreateData(Name)
    local DataObject = {}

    

    return DataObject
end

return DataService


-- Maybe do like this?

-- Project Folder when compiled on where studiodream data is saved folder (not the actual folder of studiodream)
---- Project Name (folder)
------ Datas (folder)
------- NameOfTheFile (txt or lua or table idk)